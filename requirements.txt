# Rankle - Web Infrastructure Reconnaissance Tool
# All dependencies in one file
#
# Install production only:  pip install -r requirements.txt
# Install with dev tools:   pip install -r requirements.txt --all-extras
# Or use pyproject.toml:    pip install -e ".[dev]"

# =============================================================================
# PRODUCTION DEPENDENCIES
# =============================================================================

requests>=2.31.0
dnspython>=2.4.0
beautifulsoup4>=4.12.0

# Optional (enhanced features)
python-whois>=0.9.0
ipwhois>=1.2.0

# =============================================================================
# DEVELOPMENT DEPENDENCIES
# =============================================================================

# Linting & Formatting (Ruff replaces Black, isort, flake8)
ruff>=0.8.0

# Type checking
mypy>=1.13.0
types-requests>=2.31.0
types-beautifulsoup4>=4.12.0

# Security scanning
bandit[toml]>=1.7.10
pip-audit>=2.7.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0

# Documentation
interrogate>=1.7.0

# Git hooks
pre-commit>=3.8.0
